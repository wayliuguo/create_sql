#!/usr/bin/env node
"use strict";const{program:e}=require("commander"),i=require("../libs/utils/package.config"),{configFileName:o}=require("../libs/utils/constant");e.version(i.cliVersion),e.command("init").description("初始化配置文件").action((()=>{require("../libs/init_config").initConfig()})),e.command("create").description(`根据${o} 生成sql`).action((()=>{const e=require("../libs/parse_code"),i=require("../libs/generate_sql");console.log("开始解析代码...");const o=e.parseCode();o.length>0?(console.log("开始生成SQL..."),i.generateSql(o)):console.log("未找到任何上报点，不生成SQL")})),e.parse(process.argv);
